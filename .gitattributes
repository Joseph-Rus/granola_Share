# Auto detect text files and perform LF normalization
* text=auto
# Fixed expectations the C# tests compare byte for byte.
engine/tests/StudyStash.Core.Tests/Golden/** -text
engine/**/*.cs text eol=lf
