# Auto detect text files and perform LF normalization
* text=auto
# The Python engine writes these; the C# tests compare against them byte for byte.
engine/tests/StudyStash.Core.Tests/Golden/** -text
engine/**/*.cs text eol=lf
