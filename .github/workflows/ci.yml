# The build and release pipeline for Study Stash (C# on .NET 10, one app for both roles).
#   Every push and pull request: the engine's tests on macOS, Linux and Windows; the Mac app (one universal
#   bundle, both roles' DMGs, self-tested here and again natively on an Intel runner); and the Windows app
#   (both roles' Setup.exe, self-tested, installed, updated in place, and uninstalled cleanly).
#   Push to main with a StudyStashVersion in engine/Directory.Build.props that has no tag yet: once all of the
#   above passes, publish release v<version> with the four installers and SHA256SUMS.txt. Installed copies pick
#   it up within a few hours (auto_update), or right away with `studystash update`. A version already released
#   is never published again.
name: ci

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # The C# engine (engine/): every test on each system. Golden/ holds fixed expectations the tests compare byte
  # for byte.
  engine:
    name: C# engine (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    timeout-minutes: 15
    runs-on: ${{ matrix.os }}
    env:
      DOTNET_CLI_TELEMETRY_OPTOUT: "1"
      DOTNET_NOLOGO: "1"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"
      # Whisper's smallest model, so the live tests hear real speech on each system (the app uses large-v3).
      - uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/whisper
          key: whisper-ggml-tiny-v1
      - name: Whisper tiny, for the live transcription tests
        shell: bash
        run: |
          mkdir -p "$RUNNER_TEMP/whisper"
          [ -f "$RUNNER_TEMP/whisper/ggml-tiny.bin" ] || curl -fsSL -o "$RUNNER_TEMP/whisper/ggml-tiny.bin" \
            https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-tiny.bin
          echo "STUDYSTASH_WHISPER_MODEL=$RUNNER_TEMP/whisper/ggml-tiny.bin" >> "$GITHUB_ENV"
      - name: Run tests
        run: dotnet test engine/StudyStash.slnx
        env:
          # Windows: also run the library as a real background service, from the Startup folder with no window.
          STUDYSTASH_LIVE_SERVICE: ${{ runner.os == 'Windows' && '1' || '' }}

  # The Mac app: one universal bundle for Apple silicon and Intel, started by its own launcher (D1); ad hoc
  # signed with the microphone entitlement; both roles' DMGs (D2). Proved with the real self-test (D5): real
  # windows open, the pretend microphone records, Whisper (Metal) hears the speech from inside the bundle.
  mac-app:
    name: Mac app (DMGs, self-test)
    needs: [engine]
    timeout-minutes: 25
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"
      - uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/whisper
          key: whisper-ggml-tiny-v1
      - name: Whisper tiny, for the self-test
        run: |
          mkdir -p "$RUNNER_TEMP/whisper"
          [ -f "$RUNNER_TEMP/whisper/ggml-tiny.bin" ] || curl -fsSL -o "$RUNNER_TEMP/whisper/ggml-tiny.bin" \
            https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-tiny.bin
          echo "STUDYSTASH_WHISPER_MODEL=$RUNNER_TEMP/whisper/ggml-tiny.bin" >> "$GITHUB_ENV"
      - name: Build Study Stash.app (both roles) and both DMGs
        run: sh macos/build-app.sh dist/mac
      - name: Signed, universal, and reports its version on both architectures
        run: |
          APP="dist/mac/Study Stash.app"
          codesign --verify --deep --strict --verbose=2 "$APP"
          codesign --verify --deep --strict --verbose=2 "dist/mac/library/Study Stash.app"
          lipo -info "$APP/Contents/MacOS/StudyStash" | grep -q "x86_64"
          lipo -info "$APP/Contents/MacOS/StudyStash" | grep -q "arm64"
          V=$(env -u DOTNET_ROOT "$APP/Contents/MacOS/StudyStash" version)
          [ -n "$V" ]
          softwareupdate --install-rosetta --agree-to-license 2>/dev/null || true
          [ "$(env -u DOTNET_ROOT arch -x86_64 "$APP/Contents/MacOS/StudyStash" version)" = "$V" ]
      - name: Self-test (D5) - real windows, the pretend mic, Whisper from inside the bundle
        run: sh macos/selftest.sh "dist/mac/Study Stash.app" "$RUNNER_TEMP/selftest"
      - name: Both DMGs hold the app with the right role, and no model is bundled (D6)
        run: |
          for pair in Laptop:laptop Library:library; do
            name=${pair%%:*}; role=${pair##*:}
            mnt="$RUNNER_TEMP/mnt-$name"
            mkdir -p "$mnt"
            hdiutil attach -nobrowse -readonly -noautoopen -mountpoint "$mnt" "dist/mac/Study-Stash-$name.dmg"
            /usr/libexec/PlistBuddy -c "Print :StudyStashRole" "$mnt/Study Stash.app/Contents/Info.plist" | grep -qx "$role"
            [ -L "$mnt/Applications" ]
            if find "$mnt" -iname 'ggml-*.bin' -o -name '*.bin' -size +1M | grep -q .; then
              hdiutil detach -quiet "$mnt"
              echo "a model file is bundled in Study-Stash-$name.dmg" >&2
              exit 1
            fi
            hdiutil detach -quiet "$mnt"
          done
      - uses: actions/upload-artifact@v4
        with:
          name: mac-installers
          path: |
            dist/mac/Study-Stash-Laptop.dmg
            dist/mac/Study-Stash-Library.dmg
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: mac-selftest
          path: ${{ runner.temp }}/selftest

  # The Intel self-test (D5), for real: a physical Intel Mac, so the x64 tree runs natively (mac-app already
  # checked it runs enough to print its version under Rosetta).
  mac-intel:
    name: Mac app self-test (Intel)
    needs: [mac-app]
    timeout-minutes: 15
    runs-on: macos-15-intel
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: mac-installers
          path: dist/mac
      - uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/whisper
          key: whisper-ggml-tiny-v1
      - name: Whisper tiny, for the self-test
        run: |
          mkdir -p "$RUNNER_TEMP/whisper"
          [ -f "$RUNNER_TEMP/whisper/ggml-tiny.bin" ] || curl -fsSL -o "$RUNNER_TEMP/whisper/ggml-tiny.bin" \
            https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-tiny.bin
          echo "STUDYSTASH_WHISPER_MODEL=$RUNNER_TEMP/whisper/ggml-tiny.bin" >> "$GITHUB_ENV"
      - name: Mount the laptop DMG and copy the app out (artifacts lose exec bits, so it travels as a DMG)
        run: |
          mkdir -p "$RUNNER_TEMP/mnt"
          hdiutil attach -nobrowse -readonly -noautoopen -mountpoint "$RUNNER_TEMP/mnt" dist/mac/Study-Stash-Laptop.dmg
          ditto "$RUNNER_TEMP/mnt/Study Stash.app" "$RUNNER_TEMP/Study Stash.app"
          hdiutil detach -quiet "$RUNNER_TEMP/mnt"
          codesign --verify --deep --strict "$RUNNER_TEMP/Study Stash.app"
      - name: Self-test, natively on Intel
        run: sh macos/selftest.sh "$RUNNER_TEMP/Study Stash.app" "$RUNNER_TEMP/selftest"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: mac-intel-selftest
          path: ${{ runner.temp }}/selftest

  # The Windows app: one AppId for both roles (D2/D3), self-tested (D5), installed, updated in place with a
  # silent Setup.exe, and uninstalled without touching lectures or settings outside the install folder.
  windows-app:
    name: Windows app (Setup.exe, self-test, install)
    needs: [engine]
    timeout-minutes: 25
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"
      - name: Inno Setup 6, if this image lacks it
        shell: pwsh
        run: |
          if (-not (Test-Path "${env:ProgramFiles(x86)}\Inno Setup 6\ISCC.exe")) { choco install innosetup -y --no-progress }
      - uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}\whisper
          key: whisper-ggml-tiny-v1
      - name: Whisper tiny, for the self-test
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path "$env:RUNNER_TEMP\whisper" | Out-Null
          $model = "$env:RUNNER_TEMP\whisper\ggml-tiny.bin"
          if (-not (Test-Path $model)) {
            Invoke-WebRequest "https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-tiny.bin" -OutFile $model
          }
          echo "STUDYSTASH_WHISPER_MODEL=$model" >> $env:GITHUB_ENV
      - name: Build win-x64 and win-arm64, and both Setup.exe
        shell: pwsh
        run: .\windows\build.ps1 -Out dist\windows
      - name: Reports the version from Directory.Build.props, and no model is bundled (D6)
        shell: pwsh
        run: |
          $V = (Select-String -Path engine\Directory.Build.props -Pattern '<StudyStashVersion>(.*)</StudyStashVersion>').Matches[0].Groups[1].Value
          if (-not $V) { throw "no StudyStashVersion in engine/Directory.Build.props" }
          $printed = (& "dist\windows\win-x64\StudyStash.exe" version | Out-String).Trim()
          if ($printed -ne $V) { throw "version printed '$printed', expected '$V'" }
          echo "V=$V" >> $env:GITHUB_ENV
          $bad = Get-ChildItem dist\windows -Recurse -File -Filter "*.bin" -ErrorAction SilentlyContinue | Where-Object { $_.Length -gt 1MB }
          if ($bad) { throw "a model file is bundled: $($bad.FullName -join ', ')" }
      - name: Self-test (D5)
        shell: pwsh
        run: |
          $selftestHome = "$env:RUNNER_TEMP\selftest-home"
          $out = "$env:RUNNER_TEMP\selftest"
          New-Item -ItemType Directory -Force -Path $selftestHome, $out | Out-Null
          $env:STUDYSTASH_SELFTEST = $out
          $env:STUDYSTASH_MIC_FILE = (Resolve-Path "engine\tests\StudyStash.Core.Tests\Fixtures\speech.wav").Path
          $env:STUDYSTASH_MIC_SPEED = "4"
          $env:STUDYSTASH_MODEL_FILE = $env:STUDYSTASH_WHISPER_MODEL
          $p = Start-Process -FilePath "dist\windows\win-x64\StudyStash.exe" -ArgumentList "--home", $selftestHome -PassThru
          if (-not $p.WaitForExit(480000)) { $p.Kill(); throw "Study Stash didn't exit within 480 s" }
          $lines = Get-Content "$out\selftest.txt"
          $lines | Write-Host
          if ($p.ExitCode -ne 0) { throw "exit code $($p.ExitCode)" }
          if ($lines[-1] -ne "ok") { throw "last line was not ok" }
          if ($lines -match "heard nothing|no model|recording didn't start|FAILED") { throw "the self-test reported a problem" }
      - name: The Laptop installer puts Study Stash on this account, marked laptop
        shell: pwsh
        run: |
          Start-Process "dist\windows\Study-Stash-Laptop-Setup.exe" -ArgumentList "/VERYSILENT", "/SUPPRESSMSGBOXES", "/NORESTART" -Wait
          $app = "$env:LOCALAPPDATA\Programs\Study Stash"
          if (-not (Test-Path "$app\StudyStash.exe")) { throw "Study Stash isn't installed" }
          if (-not (Test-Path "$env:APPDATA\Microsoft\Windows\Start Menu\Programs\Study Stash.lnk")) { throw "no Start Menu entry" }
          if (-not (Select-String -Path "$app\study-stash.ini" -Pattern "role\s*=\s*laptop" -Quiet)) { throw "not marked laptop" }
          $printed = (& "$app\StudyStash.exe" version | Out-String).Trim()
          if ($printed -ne $env:V) { throw "installed version '$printed', expected '$env:V'" }
      - name: The Library installer, over it, is an in-place upgrade marked library
        shell: pwsh
        run: |
          Start-Process "dist\windows\Study-Stash-Library-Setup.exe" -ArgumentList "/VERYSILENT", "/SUPPRESSMSGBOXES", "/NORESTART" -Wait
          $app = "$env:LOCALAPPDATA\Programs\Study Stash"
          if (-not (Select-String -Path "$app\study-stash.ini" -Pattern "role\s*=\s*library" -Quiet)) { throw "not marked library" }
          $entries = @(Get-ItemProperty "HKCU:\Software\Microsoft\Windows\CurrentVersion\Uninstall\*" -ErrorAction SilentlyContinue |
            Where-Object { $_.DisplayName -eq "Study Stash" })
          if ($entries.Count -ne 1) { throw "expected one uninstall entry for Study Stash, found $($entries.Count)" }
      - name: A silent update (relaunch=1) starts the app again from the same folder
        shell: pwsh
        run: |
          $app = "$env:LOCALAPPDATA\Programs\Study Stash"
          $updateHome = "$env:RUNNER_TEMP\update-home"
          New-Item -ItemType Directory -Force -Path $updateHome | Out-Null
          Start-Process -FilePath "$app\StudyStash.exe" -ArgumentList "--home", $updateHome, "--background" | Out-Null
          Start-Sleep 3
          Start-Process "dist\windows\Study-Stash-Library-Setup.exe" -ArgumentList "/VERYSILENT", "/SUPPRESSMSGBOXES", "/CLOSEAPPLICATIONS", "/relaunch=1" -Wait
          $ok = $false
          for ($i = 0; $i -lt 30; $i++) {
            if (Get-Process -Name StudyStash -ErrorAction SilentlyContinue | Where-Object { $_.Path -eq "$app\StudyStash.exe" }) { $ok = $true; break }
            Start-Sleep 1
          }
          if (-not $ok) { throw "no StudyStash.exe from $app running again within 30 s of the silent update" }
          Get-Process -Name StudyStash -ErrorAction SilentlyContinue | Stop-Process -Force
      - name: Uninstalling removes only the app; lectures and settings stay
        shell: pwsh
        run: |
          $app = "$env:LOCALAPPDATA\Programs\Study Stash"
          $settingsKeep = "$env:USERPROFILE\.study-stash\keep.txt"
          $lectureKeep = "$env:USERPROFILE\Documents\Study Stash\CS 101\keep.txt"
          New-Item -ItemType Directory -Force -Path (Split-Path $settingsKeep), (Split-Path $lectureKeep) | Out-Null
          "keep" | Out-File $settingsKeep
          "keep" | Out-File $lectureKeep
          Start-Process "$app\unins000.exe" -ArgumentList "/VERYSILENT", "/SUPPRESSMSGBOXES" -Wait
          $gone = $false
          for ($i = 0; $i -lt 60; $i++) { if (-not (Test-Path $app)) { $gone = $true; break }; Start-Sleep 1 }
          if (-not $gone) { throw "$app is still there 60 s after uninstalling" }
          if (Test-Path "$env:APPDATA\Microsoft\Windows\Start Menu\Programs\Study Stash.lnk") { throw "the Start Menu entry survived" }
          if (-not (Test-Path $settingsKeep)) { throw "settings outside the app folder were deleted" }
          if (-not (Test-Path $lectureKeep)) { throw "a lecture outside the app folder was deleted" }
      - uses: actions/upload-artifact@v4
        with:
          name: windows-installers
          path: |
            dist\windows\Study-Stash-Laptop-Setup.exe
            dist\windows\Study-Stash-Library-Setup.exe
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: windows-selftest
          path: ${{ runner.temp }}\selftest

  # Whether engine/Directory.Build.props holds a version with no tag yet: gates the release job below.
  version:
    name: version
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.v.outputs.version }}
      new: ${{ steps.v.outputs.new }}
    steps:
      - uses: actions/checkout@v4
      - id: v
        run: |
          V=$(sed -n 's:.*<StudyStashVersion>\(.*\)</StudyStashVersion>.*:\1:p' engine/Directory.Build.props)
          if [ -z "$V" ]; then echo "no StudyStashVersion in engine/Directory.Build.props" >&2; exit 1; fi
          echo "version=$V" >> "$GITHUB_OUTPUT"
          if [ -z "$(git ls-remote --tags origin "refs/tags/v$V")" ]; then
            echo "new=true" >> "$GITHUB_OUTPUT"
          else
            echo "new=false" >> "$GITHUB_OUTPUT"
          fi

  # Publish v<version> the first time it passes everything above, with exactly the four installers and their
  # checksums (D2). A version already tagged (or released) is never published again.
  release:
    name: publish release
    needs: [version, engine, mac-app, mac-intel, windows-app]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.version.outputs.new == 'true'
    timeout-minutes: 10
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: mac-installers
          path: dist
      - uses: actions/download-artifact@v4
        with:
          name: windows-installers
          path: dist
      - name: Publish v${{ needs.version.outputs.version }}
        env:
          GH_TOKEN: ${{ github.token }}
          V: ${{ needs.version.outputs.version }}
        run: |
          # Re-check the race: another push could have published this tag while this run was building.
          if git rev-parse -q --verify "refs/tags/v$V" >/dev/null || gh release view "v$V" >/dev/null 2>&1; then
            echo "v$V is already released; bump StudyStashVersion in engine/Directory.Build.props to publish a new one."
            exit 0
          fi
          (cd dist && sha256sum Study-Stash-* > SHA256SUMS.txt)
          gh release create "v$V" --target "$GITHUB_SHA" --title "Study Stash $V" --generate-notes \
            dist/Study-Stash-Laptop.dmg dist/Study-Stash-Library.dmg \
            dist/Study-Stash-Laptop-Setup.exe dist/Study-Stash-Library-Setup.exe \
            dist/SHA256SUMS.txt
